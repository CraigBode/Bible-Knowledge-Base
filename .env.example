DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db

# A single shared password that guards every page. Anyone without it is
# redirected to /login. Choose something only you know.
APP_PASSWORD=changeme
